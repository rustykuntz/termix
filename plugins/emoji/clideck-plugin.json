{
  "id": "emoji",
  "name": "Emoji",
  "version": "1.0.0",
  "apiVersion": 1,
  "enabledByDefault": true,
  "description": "A fast, searchable emoji picker that inserts into the active terminal draft.",
  "icon": "☺",
  "settings": [
    {
      "key": "shortcut",
      "label": "Emoji picker shortcut",
      "type": "shortcut",
      "default": "",
      "description": "Optional keyboard shortcut. CliDeck prevents conflicts with other plugins."
    }
  ]
}
